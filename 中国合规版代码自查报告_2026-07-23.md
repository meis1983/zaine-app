# 在呢+ 中国合规版 · 代码与功能自查报告
**日期**:2026-07-23  |  **背景**:中国区 App Store 第 4 次被拒,拒因为"触发死亡开关(dead-man switch)",07-23 拒信为中国法务依《网络安全法》做出的合规判定,App 已从中国区下架。

---

## 一、结论速览
前几轮整改把**"自动通知守护人"的代码路径**都正确闸死了(漏签 / 围栏 / 跌倒 / 手表心率),并把三张敏感功能卡(定时确认 / 围栏 / 跌倒)在 CN 版隐藏。

但自查发现 **CN 版仍有 2 个确定性的监控信号完全敞开**,加上整体"守护人查看你位置 / 状态"的产品定位,正是 Apple 中国法务判定"死亡开关 / 监控"的依据。代码层面的自动通知已修,但**产品范式没变**,所以重提审仍可能被同一条法条再次驳回。

---

## 二、✅ 已正确闸死(无需改)
| 能力 | 位置 | 闸口 |
|---|---|---|
| 漏签自动通知守护人 | `safety_service.dart:518` | CN 仅本地提醒 |
| 围栏离开自动通知 | `geofence_service.dart:340` | CN 仅本地提醒 |
| 跌倒自动通知 | `safety_service.dart:1060` | `shouldAutoNotify = notifyGuardians && !isChinaRegion` |
| 手表心率自动签到 | `health_service.dart:683` | `if (isChinaRegion && !fromWatch) return;` |
| 手机跌倒检测 | `fall_detection_service.dart:52` | CN 不注册加速度计 |
| 围栏定时检查 | `geofence_service.dart:366` | CN 直接 return |
| 三张功能卡 UI 在 CN 隐藏 | `safety_settings_page.dart:246 / 302 / 314` | `enableXxxCn || !isChinaRegion` |

---

## 三、❌ CN 版仍敞开的监控信号(确定要改)

### ① 位置共享卡未隐藏 → 持续后台定位上传(最严重)
`safety_settings_page.dart:258` 的 `LocationTrackCard` **没有任何** `isChinaRegion` / 功能开关保护,在 CN 版直接显示。用户点"开始"后:
- `safety_service.dart:732 startLocationTracking` 每 30s / 5min / 15min 抓 GPS;
- `_uploadLocationToBackend`(`:847`)持续上传后端,守护人可在 App 内 / 网页查看你的**实时轨迹**。
- 这正是"第三方持续监控某人位置"的监控本质,比死开关更敏感,且 CN 用户现在就能打开它。

### ② Info.plist 声明"始终位置"发给守护人
`ios/Runner/Info.plist:57` `NSLocationAlwaysAndWhenInUseUsageDescription`:
> "在呢需要**始终**获取您的位置,以便在您发起求助时向您的**守护人**发送位置信息。"

审核法务看到"始终 + 守护人"几乎必判监控。CN 版应改为仅 `WhenInUse`,且文案去掉"守护人 / 始终"。

### ③ 残留"自动"文案(若页面可达即违规)
- `fall_event_history_page.dart:245`「检测到跌倒后会**自动记录并通知守护者**」(若该页在 CN 仍可进入);
- `safety_features.dart:141`「手表检测到你时**自动确认**,零操作守护」(在已被隐藏的 CheckInReminderCard 内,通常不可见,但建议一并清理)。

---

## 四、🧭 根因:不是某个 bug,是产品定位
即使把①②③全修掉,CN 版本质仍是"你加守护人 → 他们能看到你的签到状态 / SOS 实时位置"。Apple 07-23 拒信是按**中国网络安全法**做的法务判定,盯的是"让他人监控 / 自动获知某人行踪与安全"这一产品范式,而不是某个具体按钮。代码闸死能消除"自动"表象,但**架构范式不改,重提审仍有被同一法条再次驳回的风险**。

SOS 属于用户主动一键发起(非自动),相对可辩护,但"实时位置推给守护圈看板"仍属监控信号,需重新评估。

---

## 五、建议路线(需你拍板)
- **A. 彻底去监控化(唯一可能过审的方向)**:CN 版砍掉一切"守护人查看你位置 / 状态"与持续定位,重定位为"个人自主签到 / 日记"App;仅保留用户主动发起的 SOS(位置只发给用户自己勾选的紧急联系人,不进"守护圈"看板)。工作量大,但是正路。
- **B. 中国区放弃上架,只做海外版**:4 次被拒 + 法务判定,中国区 App Store 基本是死路;海外版 `com.zaine.app` 已合规上架,可继续服务海外用户,中国用户走其他分发渠道。
- **C. 先修确定性泄漏(①②③)再申诉**:把最刺眼的监控信号去掉后重申,看 Apple 是否放行(**不保证**,因范式未变)。

## 六、我现在可以立刻做(你确认后执行)
- 修①:把 `LocationTrackCard` 在 CN 隐藏(或改为仅手动单次定位、不上传持续轨迹);
- 修②:`Info.plist` CN 版去掉 `NSLocationAlwaysAndWhenInUseUsageDescription`,文案改为"仅在使用时获取位置用于求助";
- 修③:清理残留"自动通知 / 自动确认"文案。

> 备注:以上为技术自查,非法律意见。中国区能否上架最终取决于 Apple 法务对产品范式的认定。
