# 中国合规版 149 出包前全面自检报告
> 日期：2026-07-23 ｜ 目标：重提审（探底线，选项 A）前的最终合规面核查
> 范围：CN 构建（`ZAI_REGION=cn`）的死开关能力、功能闸门、Watch 逻辑、权限声明、后端联动

---

## 一、构建配置（build_ipa.sh + Info.plist）

| 检查项 | 结论 | 说明 |
|---|---|---|
| Bundle ID 切换 | ✅ 通过 | `sed 's/com\.zaine\.app/com.zaine.app.cn/g'` 覆盖主 App / Watch / Extension 派生后缀 |
| IAP 签名密钥注入 | ✅ 通过 | `ZAINE_SIGN_SECRET` 默认值兜底，避免构建抛异常 |
| Info.plist 两条 sed + trap 还原 | ✅ 通过 | 位置/健康权限文案中性化，`.zai_bak` 备份 + `cleanup_cn EXIT` 还原，海外版原文不动 |
| **UIBackgroundModes** | ✅ **利好** | 仅 `remote-notification` + `fetch`，**未声明 `location` 后台模式** → 二进制不声称持续后台定位能力 |
| Info.plist 已中性化项 | ✅ 通过 | `NSLocationAlwaysAndWhenInUseUsageDescription`(原58行)、`NSHealthClinicalHealthRecordsShareUsageDescription`(原52行) |
| ⚠️ **残留叙事词（未中性化）** | ⚠️ 待定 | 见下方"残留项 R1–R3" |

### 残留叙事词（建议顺手中性化，成本≈0）
- **R1** `NSHealthShareUsageDescription`：「…以实现**生命体征守护功能**。」（"守护"）
- **R2** `NSHealthUpdateUsageDescription`：「…以实现**每日签到报平安**和经期跟踪功能。」（"报平安"）
- **R3** `UNUserNotificationCenter`：「在呢需要发送通知提醒您**定时报平安**。」（"定时报平安"）
- （可选 R4）`NSLocationWhenInUseUsageDescription`：「…向您的**联系人**发送您的位置信息」——紧急+联系人语境，可接受，非必须改。

> 注：你不认为叙事词是拒因、且位置共享本就是手动开启。是否把 R1–R3 也加进 sed 由你定；我们本就要重出包，加上等于零成本加固。

---

## 二、功能闸门（feature_flags + isChinaRegion）

- ✅ `enableCheckInReminderCn` / `enableGeoFenceCn` / `enableFallDetectionCn` **三项均为 false**。
- ✅ 所有 `isChinaRegion` 分支逐一核对，**死开关相关自动外发全部闸死或降级为本地提醒**：

| 位置 | 逻辑 | CN 行为 |
|---|---|---|
| `safety_service.dart:383` | 漏签定时提醒 `_startReminderTimer` | `!isChinaRegion` 才启动 → CN 不启动 |
| `safety_service.dart:487` | 手表辅助签到 `_tryWatchAuxiliaryCheckin` | CN → 仅本地提示，绝不自动代操作 |
| `safety_service.dart:518` | 漏签未确认 | CN → 关闭"自动通知第三方亲友" |
| `safety_service.dart:600` | 围栏离开 | CN → 仅本地提醒用户本人 |
| `safety_service.dart:1060` | 跌倒自动通知 `shouldAutoNotify` | `notifyGuardians && !isChinaRegion` → CN 关闭 |
| `geofence_service.dart:340/366` | 围栏检查 | CN `return` 早退 |
| `fall_detection_service.dart:52/209/260` | 跌倒检测/超时/确认 | CN `return` 或等待用户手动确认 |
| `health_service.dart:683` | 静默心跳/信号签到 | `isChinaRegion && !fromWatch` → 禁用自动；Watch 发起的自愿签到允许 |
| `safety_settings_page.dart:246/305/317` | 定时确认/围栏/跌倒三卡 | `enableXxxCn || !isChinaRegion` → CN 隐藏 |
| `safety_settings_page.dart:259` | **位置共享卡** | `!isChinaRegion` → CN **彻底隐藏**（修复 ①） |

**结论：死开关功能能力在 CN 二进制中已不存在。**

---

## 三、Watch App（Swift 侧）

- ✅ 全工程 swift 检索 `notifyGuardian / sendLocation / performSilent / uploadLocation / 自动通知` → **仅 `guardianCount` 展示逻辑**，无任何独立自动通知或位置上传。
- ✅ 心跳打卡经 Flutter `health_service.dart:683` 统一闸门；CN 下仅 Watch 发起的自愿签到允许，符合"用户主动"原则。

---

## 四、SOS 手动性

- ✅ SOS 为用户主动长按触发；位置分享需"**可在确认后通知**"（`contacts_page.dart:579` 活跃代码，非 `.bak` 的"会自动通知"）。属合法紧急求助，非死开关。

---

## 五、后端联动（zaine-backend）

- ⚠️ `routes/safety.py:230 POST /safety/notify-guardians`（"用户连续未确认平安→通知守护人"）**端点存在但 CN 包不调用**（客户端 `shouldAutoNotify` 已闸死），处于休眠态 → 低风险。
- ✅ `static/support.html:47` 已明确声明："通知仅在您确认后发出，不会在您无操作的情况下自动外发。"
- ✅ `privacy.html:48` 仅描述"发起求助时向指定守护人发送通知"（手动 SOS）。
- 建议：若后续想彻底消除服务端死开关痕迹，可在该端点加 `region != cn` 拒绝；但非 149 阻塞项（CN 二进制不触达）。

---

## 六、出包与重提清单

1. **出包命令**（本机 Terminal，非 WorkBuddy 终端）：
   ```bash
   ZAI_REGION=cn ZAI_BUILD_NUMBER=149 bash build_ipa.sh
   ```
   `149` 须 > 已上传的 `148`。
2. **上传**：Transporter 拖 `build/ios/ipa/在呢+.ipa`（自动匹配 cn App，规避历史"传错 App"坑）。
3. **自测确认**：CN 安全中心应只剩总览——位置共享卡 + 定时确认/围栏/跌倒三卡均不可见；Watch 无自动外发。
4. **ASC 重提前确认**：
   - 4 项可供审核（App + 订阅群组 + 月卡 + 年卡）随版本一并提交；
   - App Store 描述/关键词是否也要弱化"守护/报平安"叙事（与 R1–R3 同源决策）；
   - 隐私政策 URL 指向的页面已含"不自动外发"声明。

---

## 总评
- **死开关功能能力：已关闭 ✅**
- **唯一剩余面**：Info.plist 中 R1–R3 三条叙事词（是否中性化由你定，建议加）；后端休眠自动通知端点（低风险，非阻塞）。
- 功能层面 149 已具备重提条件；能否过审取决于苹果法务对"守护圈范式"的最终判定（代码无法消除概念本身）。
