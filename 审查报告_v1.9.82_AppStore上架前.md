# 在呢+ (Zaine) 上架前代码审查报告

**版本**: v1.9.82 (Build 34)  
**审查日期**: 2026-05-17  
**审查范围**: Flutter 前端全量代码 + iOS 配置 + App Store 合规性  

---

## 一、执行摘要

本次审查覆盖 `lib/` 下全部 Dart 源码、iOS 配置 (`Info.plist`)、依赖配置 (`pubspec.yaml`) 及关键 Widget。总体评价：

> **代码质量良好，架构清晰，用户隔离和异常处理机制较完善。但存在 3 个 P0 级问题必须在正式上架前修复，另有若干 P1/P2 级风险和优化项。**

| 等级 | 数量 | 说明 |
|------|:----:|------|
| 🔴 P0 严重 | 6 | 必须修复，否则可能导致审核被拒或安全事故 |
| 🟠 P1 高风险 | 6 | 强烈建议修复，影响稳定性或用户体验 |
| 🟡 P2 中风险 | 5 | 建议修复，存在潜在隐患 |
| 🟢 P3 优化 | 4 | 可后续迭代优化 |

---

## 二、🔴 P0 严重问题（必须修复）

### P0-1 【文案欺诈风险】订阅页仍有"同时""同步"等不实描述

**位置**: `lib/pages/subscription_page.dart`

当前营业执照未下发，紧急求助功能实际为**跳转系统拨号/短信界面**（非自动同时拨打）。但以下 3 处文案仍暗示"同时自动"能力：

| 行号 | 代码/文案 | 问题 |
|------|----------|------|
| 546 | `title: '紧急时刻，3人同时收到消息'` | "同时"暗示自动同时通知 |
| 1244 | `title: '紧急时刻，3人同步守护'` | "同步"暗示自动同步 |
| 1245 | `subtitle: '一键求助，同时拨打并短信通知前3位联系人'` | "同时拨打" |

**风险**: 用户付费后发现并非"同时自动通知"而是"逐个手动跳转系统界面"，可能引发客诉、退款潮，甚至被苹果以"误导性内容"下架。

**修复建议**:
- 546 行 → `'紧急时刻，快捷通知3位守护人'`
- 1244 行 → `'紧急时刻，快捷拨打3位守护人'`
- 1245 行 → `'一键求助，快捷拨打并短信通知前3位联系人'`

---

### P0-2 【安全】开发者模式暴露在生产环境

**位置**: `lib/widgets/developer_mode.dart`

通过连续点击版本号 3 次可进入开发者菜单，包含以下危险功能：
- **切换会员等级**（`setMembershipForDebug`）— 可绕过 IAP 直接获得付费权益
- **重置守护卡配额**（`resetGuardianCard`）— 调用后端 `/api/cards/reset-quota`
- **签到模拟器**（`showCheckInSimulator`）— 伪造连续签到天数并同步到后端
- **模拟会员过期降级**（`showMembershipExpiryDialog`）

**风险**: 虽然需要特定操作触发，但反编译 IPA 后可直接调用这些方法。用户可能在论坛/社群传播"破解教程"，导致收入损失。

**修复建议**（二选一）：
1. **方案 A（推荐）**: 上架前彻底移除 `DeveloperMode` mixin 及其所有调用，或将其编译条件包裹在 `#if DEBUG` 中
2. **方案 B**: 保留开发者菜单但移除"切换会员等级"和"重置守护卡"两个敏感功能，仅保留"查看调试信息"

---

### P0-3 【用户数据隔离】日历热力图读取全局签到历史

**位置**: `lib/pages/home_page.dart` 第 1158 行

```dart
final checkinHistory = prefs.getStringList('checkin_history') ?? [];
```

**问题**: 日历使用的是全局 key `'checkin_history'`，而 `_handleCheckIn()` 中保存时使用了**用户隔离 key** `'checkin_history_$uid'`。

**后果**: 
- 用户 A 切换账号到用户 B 后，日历显示的是 A 的签到记录
- 多账号场景下签到历史完全错乱

**修复**:
```dart
final uid = prefs.getString('user_id') ?? '';
final historyKey = uid.isNotEmpty ? 'checkin_history_$uid' : 'checkin_history';
final checkinHistory = prefs.getStringList(historyKey) ?? [];
```

---

### P0-4 【安全】IAP 签名密钥硬编码在客户端

**位置**: `lib/services/api/subscription_service.dart` 第 11 行

```dart
static const _signSecret = 'zaine-storekit-mvp-v1';
```

**风险**: 该密钥用于 HMAC-SHA256 签名验证 IAP 凭证。虽然需要前后端一致，但客户端硬编码意味着任何人反编译 IPA 即可获取密钥，可伪造签名调用 `/api/subscription/verify-receipt` 接口。

**修复建议**:
- **短期**: 将密钥移至 `.env` 文件（已在 `AppConstants` 中使用 `dotenv` 模式），编译时混淆
- **长期**: 后端改用 Apple 服务器到服务器通知（Server-to-Server Notifications）验证收据，客户端不再参与签名

---

### P0-5 【审核合规】删除账号后无法正确退出应用

**位置**: `lib/pages/settings_page.dart` 第 607-614 行

删除账号成功后调用：
```dart
SystemChannels.platform.invokeMethod('SystemNavigator.pop');
```

**问题**: iOS 上 `SystemNavigator.pop()` **不会真正退出应用**，只是返回主屏幕，应用仍在后台运行。用户重新打开 App 时可能仍看到旧数据或处于异常状态。

**风险**: 苹果审核可能认为"删除账号"功能不完整（用户删除后重开 App 发现还在）。

**修复建议**:
```dart
// 删除成功后，清除所有本地数据并导航到引导页
Navigator.of(context).pushAndRemoveUntil(
  MaterialPageRoute(builder: (_) => const OnboardingPage()),
  (route) => false,
);
```

---

### P0-6 【文案】HelpResultDialog 中 TODO 未清理

**位置**: `lib/widgets/help_result_dialog.dart` 第 310 行

```dart
// TODO: 跳转会员升级页（Phase 4 StoreKit 集成后实现）
```

**风险**: 代码中残留 TODO 通常不会导致审核失败，但如果审核人员触发此代码路径（点击"升级智能版"提示条），会看到无操作的 SnackBar，可能被认为功能不完整。

**修复**: 移除 TODO 注释，或实现跳转逻辑。

---

## 三、🟠 P1 高风险问题（强烈建议修复）

### P1-1 【稳定性】DELETE 请求无冷启动重试

**位置**: `lib/services/api_service.dart` 第 120-134 行

`get`/`post`/`put` 都使用了 `_withRetry` 自动重试，但 `delete` 方法直接调用 `http.delete` 无重试逻辑。

**后果**: 阿里云 FC 冷启动时，删除联系人/删除账号等操作可能超时失败。

**修复**: 将 `delete` 也包裹在 `_withRetry` 中。

---

### P1-2 【审核】后台模式声明与实际使用不符

**位置**: `ios/Runner/Info.plist` 第 77-81 行

```xml
<key>UIBackgroundModes</key>
<array>
    <string>fetch</string>
    <string>remote-notification</string>
</array>
```

**问题**: `fetch`（后台获取）模式被声明，但代码中没有使用 `BackgroundFetch` 或类似 API。苹果审核对未使用的后台模式较敏感。

**修复建议**: 如果确实不需要后台获取，移除 `<string>fetch</string>`。

---

### P1-3 【隐私】日志可能泄露高德 API Key

**位置**: `lib/services/platform/location_service.dart` 第 173 行

```dart
debugPrint('[LocationService] 🔍 请求逆地理编码 URL: $url');
```

**问题**: URL 中包含完整的高德 API Key。虽然 `debugPrint` 在 Release 模式下不输出，但如果用户开启 iOS 系统日志收集（如通过 Xcode 或第三方工具），可能捕获到该日志。

**修复**: 从日志中移除完整 URL，仅输出关键信息：
```dart
debugPrint('[LocationService] 🔍 请求逆地理编码: lat=$latitude, lng=$longitude');
```

---

### P1-4 【体验】求助按钮同时响应点击和长按

**位置**: `lib/pages/help_page.dart` 第 864-867 行

```dart
GestureDetector(
  onTap: _triggerHelp,
  onLongPress: _triggerHelp,
  onTapDown: (_) => HapticFeedback.mediumImpact(),
```

**风险**: 长按本意可能是"确认操作"，但此处长按和点击效果完全相同。用户在滚动页面或误触时可能意外触发求助。

**修复建议**: 移除 `onLongPress`，仅保留 `onTap`；或把 `onLongPress` 改为"需要长按 2 秒才触发"。

---

### P1-5 【合规】加密出口声明

**位置**: `ios/Runner/Info.plist` 第 38 行

```xml
<key>ITSAppUsesNonExemptEncryption</key>
<false/>
```

**问题**: 应用使用 HTTPS（TLS）进行网络通信。根据 Apple 的加密出口合规性要求：
- 如果仅使用标准 HTTPS/TLS，可以设置为 `false`
- 如果使用了自定义加密算法，需要设置为 `true`

**结论**: 当前设置 `false` 是正确的（标准 HTTPS 属于豁免范围），但建议在 App Store Connect 提交时确认自动合规性检查通过。

---

### P1-6 【一致性】版本号硬编码与 pubspec 不一致

**位置**: `lib/data/app_constants.dart` 第 9 行

```dart
static String version = '1.9.77';
```

**问题**: `pubspec.yaml` 中版本为 `1.9.82+34`，但 `app_constants.dart` 硬编码为 `1.9.77`。虽然 `initVersion()` 会从 `pubspec.yaml` 覆盖，但初始值不一致可能导致混淆。

**修复**: 将硬编码值更新为 `1.9.82`，或直接使用空字符串作为初始值。

---

## 四、🟡 P2 中风险问题（建议修复）

### P2-1 【隐私政策链接有效性未知】

**位置**: `lib/pages/settings_page.dart` 第 481 行

```dart
final uri = Uri.parse('https://zaine-api-skhntjskvp.cn-hangzhou.fcapp.run/privacy');
```

**问题**: 苹果审核要求隐私政策必须**随时可访问**。需要确认：
1. 该 URL 当前可正常访问
2. 页面内容完整（包含数据收集、使用、共享、用户权利等）
3. ICP 备案通过后需要切换到 `https://zaine.love/privacy`

---

### P2-2 【IAP 产品配置状态未知】

**位置**: `lib/services/iap_service.dart`

产品 ID:
- `com.zaine.smart_monthly`
- `com.zaine.smart_yearly`

**需要确认**:
1. App Store Connect 中是否已创建这两个订阅产品
2. 产品状态是否为"已批准销售"
3. 价格层级是否设置正确（月度 ¥9，年度 ¥88）
4. 是否配置了订阅组（Subscription Group）以支持升降级
5. 是否配置了 16 天宽限期（Billing Grace Period）

---

### P2-3 【死代码】ProfilePage 中 `_deleteProfile` 方法未使用

**位置**: `lib/pages/profile_page.dart` 第 391-447 行

**问题**: `_deleteProfile` 方法仍然存在，但 UI 中已移除"删除健康档案"按钮，此方法成为死代码。

**修复**: 移除 `_deleteProfile` 方法，减少包体积。

---

### P2-4 【性能】日历 FutureBuilder 重复读取 SharedPreferences

**位置**: `lib/pages/home_page.dart` 第 1148 行

**问题**: `_buildCheckInCalendar` 使用 `FutureBuilder<SharedPreferences>`，每次页面重建都会重新获取实例。对于日历这种高频刷新的组件，可能造成不必要的 IO。

**修复**: 将 `SharedPreferences` 实例缓存在 State 中，通过 `didUpdateWidget` 或 `setState` 更新。

---

### P2-5 【用户体验】重置所有数据未清除头像文件

**位置**: `lib/pages/settings_page.dart` 第 1361-1371 行

**问题**: `_resetAllData` 清除了所有 SharedPreferences 键值，但**没有删除本地头像文件**（`avatar_*.jpg`、`avatar.png`）。重置后头像文件残留占用存储空间。

**修复**: 在重置逻辑中加入文件清理：
```dart
final dir = await getApplicationDocumentsDirectory();
final avatarFile = File('${dir.path}/avatar.png');
if (await avatarFile.exists()) await avatarFile.delete();
// 清理所有 avatar_*.jpg
```

---

## 五、🟢 P3 优化建议（可后续迭代）

### P3-1 【性能】减少 SnackBar 重复代码

全项目有大量重复的 SnackBar 显示逻辑（成功/失败/警告），建议封装为统一工具方法：
```dart
AppSnackBar.success(context, '保存成功');
AppSnackBar.error(context, '网络异常');
```

### P3-2 【依赖更新】

以下依赖可升级至最新版本：
| 依赖 | 当前 | 建议升级 |
|------|:----:|----------|
| `intl` | `^0.18.1` | `^0.19.0` |
| `http` | `^1.1.0` | `^1.2.0+` |
| `image` | `^4.5.1` | `^4.9.0+` |

### P3-3 【测试覆盖】

当前项目缺少单元测试和集成测试。建议至少为以下核心逻辑添加测试：
- `MembershipService` 等级判断逻辑
- `ApiService._withRetry` 重试机制
- `LocationService` 权限状态转换
- `SyncService` 数据同步策略

### P3-4 【可访问性】

部分按钮和交互元素缺少语义标签（`Semantics`），建议为视障用户优化：
- 签到按钮添加语义标签："签到按钮，已连续签到 X 天"
- 紧急求助按钮添加语义标签："紧急求助，点击后5秒倒计时"

---

## 六、App Store 审核合规检查清单

| 审核要求 | 状态 | 说明 |
|----------|:----:|------|
| 应用内购买使用 Apple IAP | ✅ | 使用 `in_app_purchase` 插件 |
| 提供恢复购买功能 | ✅ | 订阅页有"恢复购买"按钮 |
| 订阅条款说明 | ✅ | 页面底部有完整条款 |
| 隐私政策可访问 | ⚠️ | 链接已配置，需确认页面可用 |
| 用户可删除账号 | ✅ | 设置页有"删除账号"，二次确认 |
| 未使用后台模式 | ⚠️ | `fetch` 模式可能未使用 |
| 无未完成的 TODO | ⚠️ | `help_result_dialog.dart` 有 TODO |
| 无测试/调试代码暴露 | ❌ | 开发者菜单可触发 |
| 准确的功能描述 | ❌ | 订阅页有"同时"文案 |
| 加密出口合规 | ✅ | `ITSAppUsesNonExemptEncryption = false` |
| 位置权限使用说明 | ✅ | `Info.plist` 有完整的 `NSLocation...` 描述 |
| 相机/相册权限说明 | ✅ | `Info.plist` 有 `NSCameraUsageDescription` 等 |
| 通知权限说明 | ✅ | `Info.plist` 有 `UNUserNotificationCenter` |
| 联系人权限说明 | ✅ | `Info.plist` 有 `NSContactsUsageDescription` |

---

## 七、功能模块健康度评分

| 模块 | 评分 | 说明 |
|------|:----:|------|
| 登录/注册 | ⭐⭐⭐⭐⭐ | 用户隔离完善，静默登录流程完整 |
| 健康档案 | ⭐⭐⭐⭐⭐ | 数据隔离、头像压缩、后端同步均完善 |
| 紧急联系人 | ⭐⭐⭐⭐⭐ | 双写同步、排序、乐观更新机制完善 |
| 紧急求助 | ⭐⭐⭐⭐☆ | 功能完整，但按钮误触风险待优化 |
| 签到系统 | ⭐⭐⭐⭐☆ | 用户隔离完善，但日历历史 key 有 bug |
| 守护圈 | ⭐⭐⭐⭐⭐ | 三态标签、头像缓存、平安确认均完善 |
| IAP/订阅 | ⭐⭐⭐☆☆ | 文案不一致，开发者模式暴露，产品配置待确认 |
| 主题系统 | ⭐⭐⭐⭐⭐ | 三主题切换，全局颜色管理完善 |
| 设置/账号 | ⭐⭐⭐⭐☆ | 删除账号退出逻辑待优化 |
| 通知系统 | ⭐⭐⭐⭐⭐ | 签到提醒+断签预警，递进式设计 |

---

## 八、修复优先级建议

### 第一阶段（上架前必做）
1. 修复 P0-1 订阅页文案（"同时"→"快捷"）
2. 修复 P0-2 移除/加固开发者模式
3. 修复 P0-3 日历历史 key 用户隔离
4. 修复 P0-4 IAP 签名密钥移至 .env
5. 修复 P0-5 删除账号后导航到引导页
6. 修复 P0-6 移除 TODO

### 第二阶段（上架前建议做）
7. 修复 P1-1 DELETE 请求加冷启动重试
8. 修复 P1-2 移除未使用的 `fetch` 后台模式
9. 修复 P1-3 逆地理编码日志脱敏
10. 修复 P1-4 求助按钮仅响应点击
11. 修复 P1-6 版本号一致性

### 第三阶段（上架后可迭代）
12. 确认 P2-1 隐私政策页面可用性
13. 确认 P2-2 IAP 产品配置状态
14. P2-3 / P2-4 / P2-5 死代码清理和性能优化
15. P3 优化建议逐步实施

---

## 九、审查文件清单

已审查的关键文件：
- `lib/data/app_constants.dart`
- `lib/main.dart`
- `lib/services/api_service.dart`
- `lib/services/iap_service.dart`
- `lib/services/membership_service.dart`
- `lib/services/api/subscription_service.dart`
- `lib/services/api/auth_service.dart`
- `lib/services/api/sync_service.dart`
- `lib/services/api/contact_service.dart`
- `lib/services/api/checkin_service.dart`
- `lib/services/api/peace_service.dart`
- `lib/services/api/card_service.dart`
- `lib/services/api/notify_service.dart`
- `lib/services/api/user_service.dart`
- `lib/services/platform/location_service.dart`
- `lib/services/deep_link_service.dart`
- `lib/services/silent_login_service.dart`
- `lib/pages/home_page.dart`
- `lib/pages/profile_page.dart`
- `lib/pages/help_page.dart`
- `lib/pages/contacts_page.dart`
- `lib/pages/guardian_page.dart`
- `lib/pages/subscription_page.dart`
- `lib/pages/settings_page.dart`
- `lib/pages/onboarding_page.dart`
- `lib/widgets/help_result_dialog.dart`
- `lib/widgets/developer_mode.dart`
- `lib/theme/theme_helper.dart`
- `ios/Runner/Info.plist`
- `pubspec.yaml`

---

*报告由代码审查自动生成，基于对项目源码的静态分析。*
