# 在呢App 版本管理说明

## 📦 备份位置

### 1. Git 版本控制（主备份）
- **位置**: `zaine_app/.git/` 文件夹
- **内容**: 所有历史版本、提交记录、tag
- **优点**: 精确到每一次改动，可随时对比差异

### 2. 物理备份（保险备份）
- **位置**: `zaine_app_backups/zaine_app_v1.0.0_20260420_2128.tar.gz`
- **内容**: 完整的项目文件（不含 build/Pods 等生成文件）
- **用途**: 万一 Git 仓库损坏，可从 tar.gz 解压恢复

---

## 🔄 一键回滚

### 方式1：使用脚本（推荐）
```bash
cd /Users/meixulin/WorkBuddy/20260407202444/zaine_app
./rollback.sh v1.0.0
```

### 方式2：手动命令
```bash
git reset --hard v1.0.0    # 回滚到 v1.0.0
git reset --hard v1.1.0    # 回滚到 v1.1.0（如果有）
```

---

## 📝 工作流程（以后每次改动）

### 改之前
```bash
git checkout -b feature-xxx    # 创建新分支改，保险
```

### 改完确认OK
```bash
git add -A
git commit -m "feat: xxx 功能"
git tag v1.1.0                # 打 tag，作为回滚点
git checkout main
git merge feature-xxx         # 合并到 main
```

### 改崩了想回退
```bash
./rollback.sh v1.0.0          # 一键回滚到上一个可用版本
```

---

## 📊 当前版本

| 版本 | Tag | Commit | 说明 |
|------|-----|--------|------|
| v1.0.0 | ✅ | 3108b4c | 4月20日基础版本（确认可用） |

---

## ⚠️ 注意事项

1. **回滚会丢失未提交的改动** → 改之前先 `git add` + `git commit`
2. **tag 是只读的** → 一旦打好就不会变，安全可靠
3. **物理备份定期更新** → 重大版本发布时重新打包 tar.gz

